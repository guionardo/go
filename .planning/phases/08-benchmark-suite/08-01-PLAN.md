---
phase: 08-benchmark-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cache/bench_test.go
autonomous: true
requirements:
  - BENCH-01

must_haves:
  truths:
    - The mem provider thundering-herd benchmark runs and produces statistically meaningful results
    - The benchmark demonstrates the setter running once (singleflight) vs N times (naive) under concurrent load
    - All 5 providers have Docker-gated herd benchmarks when Docker is available
    - Setter invocation count, elapsed time, allocs/op, and bytes/op are reported
  artifacts:
    - cache/bench_test.go (herd section)
  key_links:
    - The b.Run subtest hierarchy must nest inside BenchmarkSingleflightGetOrSet
    - The skipIfNoDocker helper must be used by Docker-backed subtests
    - Atomic.Int64 counter must be reset per b.Iteration to avoid pollution across b.N cycles
---

<objective>
Create the thundering-herd singleflight benchmark in `cache/bench_test.go` that demonstrates the dedup win under concurrent load.

Purpose: Quantify the singleflight dedup benefit — many concurrent misses on the same key should run the setter exactly once instead of N times. This is the primary performance justification for the singleflight GetOrSet integration. Multiple concurrency levels show how the win scales with contention.
Output: `cache/bench_test.go` containing the `BenchmarkSingleflightGetOrSet` benchmark function with mem provider (always runs) and Docker-gated subtests for redis, valkey, memcache, and postgres providers.
</objective>

<execution_context>
@/Users/guionardo/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/guionardo/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/guionardo/dev/go/.planning/PROJECT.md
@/Users/guionardo/dev/go/.planning/ROADMAP.md
@/Users/guionardo/dev/go/.planning/STATE.md
@/Users/guionardo/dev/go/.planning/REQUIREMENTS.md

# Existing code to read
@/Users/guionardo/dev/go/cache/singleflight.go
@/Users/guionardo/dev/go/cache/concrete_cache.go
@/Users/guionardo/dev/go/cache/cache.go
@/Users/guionardo/dev/go/cache/mem/mem.go
@/Users/guionardo/dev/go/cache/singleflight_test.go
@/Users/guionardo/dev/go/cache/cache_e2e_test.go
@/Users/guionardo/dev/go/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cache/bench_test.go with thundering-herd benchmark (mem provider)</name>
  <files>cache/bench_test.go</files>
  <read_first>
    cache/singleflight.go — SingleflightGetOrSet.Do signature (get, set, setter closures)
    cache/cache.go — Cache and BatchCache interfaces
    cache/concrete_cache.go — concreteCache.GetOrSet delegates to sf.Do
    cache/mem/mem.go — mem.New returns BatchCache, uses singleflight via concreteCache
    cache/singleflight_test.go — Existing concurrent test patterns (sync.WaitGroup + atomic.Int64)
  </read_first>
  <action>
    Create `cache/bench_test.go` in package `cache_test` with:

    1. Package declaration and imports:
       - `context`, `fmt`, `sync`, `sync/atomic`, `testing`, `time`
       - `github.com/guionardo/go/cache`
       - `github.com/guionardo/go/cache/mem`
       - For Docker-backed subtests (Task 2): Import `github.com/testcontainers/testcontainers-go`,
         testcontainers modules (redis, postgres), and `wait`.
       - For valkey subtests: import `github.com/testcontainers/testcontainers-go`
       - For memcache subtests: import `github.com/testcontainers/testcontainers-go` and `wait`

    2. `skipIfNoDocker` helper function:
       - Name: `skipIfNoDocker(t testing.TB, reason string)`
       - Behavior: attempts `exec.Command("docker", "info").Run()` and calls `t.Skip(reason)` if it fails
       - Uses `os/exec` import
       - Place at the top of the file, before any benchmark functions

    3. `BenchmarkSingleflightGetOrSet` function:
       - Signature: `func BenchmarkSingleflightGetOrSet(b *testing.B)`
       - Defines concurrency levels: `[]int{10, 50, 100, 500}` (per D-02)
       - For each concurrency level `n`, runs two sub-benchmarks:
         a. `b.Run(fmt.Sprintf("Concurrency%d/naive", n), ...)` — no dedup
         b. `b.Run(fmt.Sprintf("Concurrency%d/singleflight", n), ...)` — via GetOrSet

    4. Naive path implementation (no dedup):
       - Create a local `sync.Mutex`-protected `map[string]string`
       - `naiveGetOrSet` closure: lock → get → if miss, compute via setter → store → unlock
       - Per b.Iteration:
         - Create a unique key for this iteration: `fmt.Sprintf("herd_naive_%d_%d", n, i)`
         - `var setterRuns atomic.Int64`
         - Fan out `n` goroutines via `sync.WaitGroup`, each calling `naiveGetOrSet`
         - Wait for all goroutines
         - After `b.StopTimer()`: `b.ReportMetric(float64(setterRuns.Load())/float64(b.N), "setter_runs/op")`
       - Setter: `setterRuns.Add(1)` then `time.Sleep(time.Millisecond)` then return `"computed"`

    5. Singleflight path implementation:
       - `c := mem.New[string, string](b.Context())` then `defer c.Close()`
       - Per b.Iteration:
         - Create a unique key: `fmt.Sprintf("herd_sf_%d_%d", n, i)`
         - `var setterRuns atomic.Int64`
         - Fan out `n` goroutines via `sync.WaitGroup`, each calling `c.GetOrSet(b.Context(), key, setter)`
         - Setter: same as naive (Add(1), Sleep(Millisecond), return "computed")
         - Wait for all goroutines
         - After `b.StopTimer()`: `b.ReportMetric(float64(setterRuns.Load())/float64(b.N), "setter_runs/op")`
       - Use `b.ResetTimer()` before the goroutine fan-out loop

    6. **Important design choices (documented in comments):**
       - Unique key per iteration avoids cached-key cross-contamination between b.N cycles
       - `benchmarkHerdSingleflight` helper function to avoid code duplication with Docker-backed subtests
       - `time.Sleep(time.Millisecond)` simulates a real setter workload (e.g., DB query, API call)
       - `setter_runs/op` custom metric: naive should show ~N, singleflight should show ~1

    7. Code structure — extract helpers to avoid duplication:
       ```go
       func benchmarkHerdNaive(b *testing.B, n int) {
           var mu sync.Mutex
           store := map[string]string{}
           naiveGetOrSet := func(key string, setter func() string) string {
               mu.Lock()
               defer mu.Unlock()
               if v, ok := store[key]; ok {
                   return v
               }
               v := setter()
               store[key] = v
               return v
           }
           // ... b.N loop with goroutine fan-out
       }

       func benchmarkHerdSingleflight(b *testing.B, n int, c cache.BatchCache[string, string]) {
           // ... b.N loop with goroutine fan-out calling c.GetOrSet
       }
       ```

    Do NOT include any batch benchmarks (MGet/MSet/MDel) in this file — those go in Plan 02.
  </action>
  <verify>
    <automated>go test -bench="BenchmarkSingleflightGetOrSet/Concurrency10/singleflight" -benchtime=100ms -count=1 -benchmem ./cache/ 2>&1</automated>
  </verify>
  <done>
    - `cache/bench_test.go` exists with `BenchmarkSingleflightGetOrSet` function
    - Both naive and singleflight subtests run for concurrency level 10
    - `skipIfNoDocker` helper compiles (called by next task)
    - `go vet ./cache/` passes
    - `make coverage-quick` passes (benchmark code is not measured for coverage)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Docker-gated thundering-herd benchmark subtests for all providers</name>
  <files>cache/bench_test.go</files>
  <read_first>
    cache/cache_e2e_test.go — E2E Docker container startup patterns (redis, valkey, memcache, postgres)
    cache/memcache/memcache.go — memcache.New with WithServers
    cache/redis/redis.go — redis.New with WithAddr
    cache/valkey/valkey.go — valkey.New with WithAddr
    cache/postgres/postgres.go — postgres.New with WithConnString
  </read_first>
  <action>
    Add the following after the mem benchmark section in `BenchmarkSingleflightGetOrSet`:

    1. For each Docker-backed provider (redis, valkey, memcache, postgres), add a sub-benchmark:
       ```
       b.Run("redis", func(b *testing.B) {
           skipIfNoDocker(b, "redis benchmark requires Docker")
           // Start container, create provider, call benchmarkHerdSingleflightConcurrencyLoop
       })
       ```

    2. Each Docker-backed subtest:
       a. Calls `skipIfNoDocker(b, "reason")` first
       b. Starts the appropriate test container using testcontainers-go (same pattern as cache_e2e_test.go)
          - redis: `tcredis.RunContainer` with `redis:7-alpine`
          - valkey: `testcontainers.GenericContainer` with `valkey/valkey:8-alpine`
          - memcache: `testcontainers.GenericContainer` with `memcached:1-alpine`
          - postgres: `tcpostgres.RunContainer` with `postgres:16-alpine`
       c. Gets the mapped port and host
       d. Creates the provider with appropriate configuration
       e. Calls `benchmarkHerdSingleflightConcurrencyLoop(b, provider)` — a helper that loops over concurrency levels and calls `benchmarkHerdSingleflight` for each
       f. Cleans up: `provider.Close()` and container termination via `defer`

    3. Container lifecycle:
       - Container is created inside the benchmark subtest function
       - Provider is created from the container's connection info
       - Container is NOT terminated explicitly — it will be garbage collected by testcontainers
         (following the existing E2E pattern)

    4. `benchmarkHerdSingleflightConcurrencyLoop` helper:
       - Loops over `[]int{10, 50, 100, 500}`
       - For each `n`, calls `b.Run(fmt.Sprintf("Concurrency%d", n), func(b *testing.B) { benchmarkHerdSingleflight(b, n, provider) })`

    5. Import grouping: add `"os/exec"` for `skipIfNoDocker`. Add testcontainers imports alongside the stdlib and third-party groups.

    **Important:** The Docker-backed subtests are gated by `skipIfNoDocker` and will be skipped when Docker is unavailable (per D-08/D-09). The mem benchmark always runs (per D-10).
  </action>
  <verify>
    <automated>go test -bench="BenchmarkSingleflightGetOrSet/redis/Concurrency10" -benchtime=100ms -count=1 -benchmem ./cache/ 2>&1</automated>
  </verify>
  <done>
    - `BenchmarkSingleflightGetOrSet` includes Docker-gated subtests for redis, valkey, memcache, and postgres
    - Each Docker subtest conditionally skips when Docker is unavailable
    - `go vet ./cache/` passes
    - Running with Docker available: non-mem subtests execute and return meaningful results
    - Running without Docker: Docker subtests are skipped, mem subtests still run
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code→Docker daemon | Benchmark code starts containers via Docker socket — untrusted image registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | Docker image pulls | medium | mitigate | Pin specific image tags (redis:7-alpine, valkey/valkey:8-alpine, memcached:1-alpine, postgres:16-alpine) — never use `latest`. |
| T-08-02 | Denial of Service | Long-running benchmarks | low | accept | Benchmarks run locally under developer control; `-benchtime` flag limits runtime. |
| T-08-SC | Tampering | Package installs | low | accept | All dependencies already used in project (`testcontainers-go`, `tcredis`, `tcpostgres`). No new packages introduced. |

## Package Legitimacy Audit

All imports already exist in go.mod (testcontainers-go, tcredis, tcpostgres, mock, redis, valkey, memcache, postgres client). No new packages introduced. Packages treated as [ASSUMED] per existing project usage.
</threat_model>

<verification>
1. `go vet ./cache/` passes
2. `go test -bench="BenchmarkSingleflightGetOrSet" -benchtime=100ms -count=1 -benchmem ./cache/` runs without errors
3. All concurrency levels (10, 50, 100, 500) produce output in both naive and singleflight paths
4. `make coverage-quick` passes (benchmark code is excluded from coverage thresholds)
</verification>

<success_criteria>
- `BenchmarkSingleflightGetOrSet` reports meaningful metrics for all concurrency levels
- Naive path shows setter_runs/op ≈ N (e.g., ~10 for Concurrency10)
- Singleflight path shows setter_runs/op ≈ 1 (e.g., ~1 for Concurrency10)
- Docker-backed subtests either run (Docker available) or skip cleanly (Docker unavailable)
- All Go toolchain checks (vet, build) pass
</success_criteria>

<output>
Create `08-01-SUMMARY.md` when done
</output>
