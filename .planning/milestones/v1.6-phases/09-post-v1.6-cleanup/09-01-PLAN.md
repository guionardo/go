---
phase: 09-post-v1.6-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/provider.go
  - config/config_test.go
  - httptest_mock/handler.go
  - httptest_mock/request.go
  - httptest_mock/handler_test.go
  - httptest_mock/request_test.go
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - Config provider exposes an HTTP handler that accepts PATCH/POST JSON to update configuration
    - httptest_mock uses http.ServeMux for path-based mock routing instead of all-mock iteration
    - httptest_mock header matching normalizes keys to lowercase before comparison, handling Windows transport variances
  artifacts:
    - config/provider.go: HTTPHandler() method
    - httptest_mock/handler.go: mux field, rebuildMux(), ServeHTTP delegation
    - httptest_mock/request.go: matchHeaders lowercase normalization
  key_links:
    - HTTPHandler writes to mutex — concurrent GetConfiguration readers get consistent state
    - ServeMux rebuild on AddMocks — dynamic mock registration without stale mux
    - matchHeaders normalization covers both mock definition keys and incoming request keys
---

<objective>
Resolve 3 outstanding cleanup todos across config and httptest_mock packages for v1.6.

Purpose: Close tech debt items identified during v1.6 development — config HTTP update endpoint,
efficient ServeMux-based mock routing, and cross-platform header matching fix.
Output: 3 independent feature additions/fixes, each with tests passing `make coverage-quick`.
</objective>

<execution_context>
@/Users/guionardo/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/guionardo/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-post-v1.6-cleanup/09-CONTEXT.md
@config/provider.go
@config/config_test.go
@httptest_mock/handler.go
@httptest_mock/request.go
@httptest_mock/handler_test.go
@httptest_mock/request_test.go
@httptest_mock/setup.go
@httptest_mock/mock.go
@httptest_mock/interfaces.go
@httptest_mock/builder.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config HTTP endpoint — add HTTPHandler() method to Provider[T]</name>
  <files>config/provider.go, config/config_test.go</files>
  <action>
    Add an `HTTPHandler()` method on `Provider[T]` that returns `http.Handler`.

    In `config/provider.go`:
    1. Add `"encoding/json"` and `"net/http"` to the import block.
    2. Add the method:
       ```go
       // HTTPHandler returns an http.Handler that accepts PATCH/POST requests
       // with a JSON body to update the configuration. Thread-safe via Provider's
       // internal write lock. Responds with 200 OK and the updated configuration
       // on success, 400 on invalid JSON, 405 on wrong method, 422 on validation failure.
       func (p *Provider[T]) HTTPHandler() http.Handler {
           return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
               if r.Method != http.MethodPatch && r.Method != http.MethodPost {
                   http.Error(w, "method not allowed", http.StatusMethodNotAllowed)
                   return
               }

               var cfg T
               if err := json.NewDecoder(r.Body).Decode(&cfg); err != nil {
                   http.Error(w, "invalid JSON: "+err.Error(), http.StatusBadRequest)
                   return
               }

               if err := p.UpdateConfiguration(cfg); err != nil {
                   http.Error(w, err.Error(), http.StatusUnprocessableEntity)
                   return
               }

               w.Header().Set("Content-Type", "application/json")
               w.WriteHeader(http.StatusOK)
               if err := json.NewEncoder(w).Encode(cfg); err != nil {
                   slog.Error("failed to encode configuration response", "error", err)
               }
           })
       }
       ```

    In `config/config_test.go`:
    3. Add imports for `"encoding/json"`, `"net/http"`, `"net/http/httptest"`, `"strings"`.
    4. Add tests:
       - `TestProvider_HTTPHandler_method_not_allowed` — GET returns 405.
       - `TestProvider_HTTPHandler_invalid_json` — PATCH with bad JSON body returns 400.
       - `TestProvider_HTTPHandler_successful_patch` — PATCH with valid JSON body returns 200 + updated config.
       - `TestProvider_HTTPHandler_post_also_accepted` — POST also works and returns 200.
       - `TestProvider_HTTPHandler_validation_error` — PATCH with invalid config (e.g., empty required field) returns 422.
       - Each test creates a provider via `NewProvider[testConfig]()`, uses `httptest.NewServer` + `httptest.NewRecorder`.

    Use `testify` assertions per project convention. Follow existing `config_test.go` patterns —
    use `testConfig` struct with `Name` required field for validation tests.
  </action>
  <verify>
    <automated>cd /Users/guionardo/dev/go && go test ./config/ -run TestProvider_HTTPHandler -v -count=1</automated>
  </verify>
  <done>
    - All HTTP handler tests pass
    - PATCH/POST with valid JSON returns 200 + updated config
    - GET returns 405, bad JSON returns 400, invalid config returns 422
    - Updated config persists across subsequent GetConfiguration calls
  </done>
</task>

<task type="auto">
  <name>Task 2: httptest_mock ServeMux routing — replace manual for-loop with http.ServeMux</name>
  <files>httptest_mock/handler.go, httptest_mock/handler_test.go</files>
  <action>
    Replace the mock-by-mock for-loop iteration in `ServeHTTP` with `http.ServeMux` pattern-based
    routing. Each mock's method+path is registered as a mux pattern pointing to a handler that
    iterates only mocks with that path.

    Per D-05 (ServeMux routing decision):
    - Backward compatible — `AddMocks` interface unchanged
    - Partial match detection still works via path-grouped mux handler

    In `httptest_mock/handler.go`:
    1. Add a `mux *http.ServeMux` field to the `MockHandler` struct.
    2. Add a `rebuildMux()` method:
       - Group s.mocks by their pattern string `"METHOD /path"`.
       - For non-`*Mock` implementations of `Mocker`, collect into a `"fallback"` group.
       - Create a new `http.ServeMux`.
       - For each group, register `mux.HandleFunc(pattern, handler)` where the handler
         iterates only the mocks in that group, running the same matching logic as the
         current `ServeHTTP` for-loop body.
       - Register fallback mocks with `mux.HandleFunc("/", fallbackHandler)`.
       - Set `s.mux = newMux`.
    3. In `ServeHTTP`:
       - After acquiring the read lock, if `s.mux == nil`, call `s.rebuildMux()`.
       - Replace the mock-iteration for-loop with `s.mux.ServeHTTP(w, r)`.
       - Keep the panic recovery and pre-response hooks unchanged.
       - If `s.mux.ServeHTTP` returns 404 (no mux handler matched), log "no mock matched"
         and return 404 (the mux already handles this for unregistered paths).
    4. Extract the existing for-loop body into a private helper `handleMockGroup(mocks []Mocker, w, r)`
       that does the iteration, partial match collection, and response writing — identical behavior
       to the current `ServeHTTP` body.
    5. In `AddMocks`:
       - After `s.mocks = append(s.mocks, mocks...)`, call `s.rebuildMux()`.
       - Keep `s.Validate()` call.
    6. Remove the mock-iteration loop from `ServeHTTP`. The new flow: mux routes by method+path,
       then the grouped handler checks full match criteria.

    In `httptest_mock/handler_test.go`:
    7. Run existing tests unchanged — they should all pass with the new routing.
    8. No test modifications needed unless a test explicitly depended on the iteration order
       across different paths (fix any by adjusting mock expectations, not rolling back routing).

    Implementation notes:
    - Go 1.22+ ServeMux supports `METHOD /path` patterns — use `"POST /api/v1/users/{id}"`
      format directly. The mux handles path parameter extraction automatically.
    - For mocks with non-`*Mock` Mocker implementations, use a fallback group that matches all paths.
      Detect `*Mock` via type assertion: `if m, ok := mock.(*Mock); ok { ... }`.
    - When registering handlers, the group handler closure captures a copy of the mock slice.
    - The mux is rebuilt under write lock (in `AddMocks`/`WithRequests`/setup), but read in
      `ServeHTTP` under read lock — safe because `ServeHTTP` only reads `s.mux` after lock.
  </action>
  <verify>
    <automated>cd /Users/guionardo/dev/go && go test ./httptest_mock/ -v -count=1 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All existing httptest_mock tests pass (no behavioral changes)
    - `AddMocks` after initial setup correctly rebuilds the mux and serves new mocks
    - `make coverage-quick` passes for httptest_mock package
  </done>
</task>

<task type="auto">
  <name>Task 3: httptest_mock Windows header matching — normalize keys to lowercase in matchHeaders</name>
  <files>httptest_mock/request.go, httptest_mock/request_test.go</files>
  <action>
    Fix `matchHeaders` in `request.go` to normalize ALL header keys to lowercase before comparison,
    ensuring Windows HTTP transport header variances (e.g., `Api-Key` sent as `Api_key`) don't
    cause false negatives.

    Per D-06 (Windows header fix decision):
    - Root cause: `http.DefaultClient` on Windows may not canonicalize before sending
    - Fix: normalize incoming request headers to lowercase BEFORE lookup
    - Always compare lowercase keys

    In `httptest_mock/request.go`, update `matchHeaders`:
    1. Normalize the expected header key first: `normalizedKey := strings.ToLower(strings.ReplaceAll(key, "_", "-"))`
       (the current code does this already as `normalized` but rename for clarity).
    2. When iterating `req.Header`, normalize each request header key the same way:
       `headerKey := strings.ToLower(strings.ReplaceAll(k, "_", "-"))`.
    3. Compare `headerKey == normalizedKey` (lowercase-to-lowercase, both underscore→hyphen).
    4. If matched, store the value in `readData` using the normalized key:
       `r.readData[readDataHeaderPrefix+normalizedKey] = v[0]`.
       This makes `GetHeaderValue("api-key")` and `GetHeaderValue("Api-Key")` both find the value.
    5. Update the match-log message to show the actual request value found (or empty) alongside
       the expected value, not just the key:
       `r.matchLog = append(r.matchLog, fmt.Sprintf("%s HEADER %s: expected %q, got %q", noMatchEmoji, normalizedKey, value, queryValue))`.
    6. Ensure empty-string header values are handled: if `v[0]` is empty string but the expected
       value is also empty, consider it a match. If expected is non-empty and found is empty, no match.

    In `httptest_mock/request_test.go`:
    7. Add `TestRequest_matchHeaders` with cases:
       - Exact match (canonical key, same case)
       - Case-insensitive match (expected `Api-Key`, request has `api_key`)
       - Underscore-to-hyphen match (expected `Api_Key`, request has `Api-Key`)
       - Windows-style non-canonical match (expected `Api-Key`, request has `Api_key`)
       - Missing header returns false
       - Multiple headers with same normalized key picks first match
       - Empty header value matches empty expected value

    Pattern: create test requests with `httptest.NewRequest` and set headers directly on the map
    (e.g., `req.Header["Api_key"] = []string{"test_value"}`) to simulate Windows non-canonical transport,
    bypassing Go's automatic canonicalization in `Header.Set()`.
  </action>
  <verify>
    <automated>cd /Users/guionardo/dev/go && go test ./httptest_mock/ -run TestRequest_matchHeaders -v -count=1</automated>
  </verify>
  <done>
    - All matchHeaders tests pass, including Windows non-canonical header keys
    - mock.GetHeaderValue returns values for both normalized and non-normalized key lookups
    - `make coverage-quick` shows no regression in coverage
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP → Config Provider | Untrusted HTTP request crosses into config update path |
| HTTP → httptest_mock | Test HTTP request hits mock router (tests only, not production) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Spoofing | config HTTPHandler | low | mitigate | Validate JSON body against config struct schema via Provider's existing validation |
| T-09-02 | Tampering | config HTTPHandler | low | mitigate | PATCH/POST only (GET/other rejected); method check before any body parsing |
| T-09-03 | DoS | config HTTPHandler | low | accept | Handler is for admin/config tooling, not public-facing; rate limiting is caller's concern |
| T-09-SC | Tampering | go modules | low | mitigate | No new dependencies added |
</threat_model>

<verification>
1. `go test ./config/ -v -count=1` — all config tests pass
2. `go test ./httptest_mock/ -v -count=1` — all httptest_mock tests pass
3. `make coverage-quick` — thresholds met (packages ≥80%, files ≥70%, total ≥75%)
</verification>

<success_criteria>
1. Config package: `HTTPHandler()` method works end-to-end — PATCH/POST JSON updates config, invalid input returns appropriate errors
2. httptest_mock: ServeMux-based routing passes all existing tests with no behavioral changes
3. httptest_mock: Header matching handles Windows non-canonical header keys correctly
4. `make coverage-quick` passes with no regression
</success_criteria>

<output>
Create `.planning/phases/09-post-v1.6-cleanup/09-01-SUMMARY.md` when done
</output>
