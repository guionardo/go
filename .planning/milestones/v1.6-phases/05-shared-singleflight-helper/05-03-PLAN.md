---
phase: 05-shared-singleflight-helper
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [cache/cache.go, cache/doc.go, cache/cache_e2e_test.go, cache/mem/mem.go, cache/redis/redis.go, cache/valkey/valkey.go, cache/memcache/memcache.go, cache/postgres/postgres.go, cache/mem/mem_test.go, cache/redis/redis_test.go, cache/valkey/valkey_test.go, cache/memcache/memcache_test.go, cache/postgres/postgres_test.go]
autonomous: true
requirements: [SF-02]

must_haves:
  truths:
    - "The Cache interface GetOrSet accepts a ctx-aware setter (D-03): setter func(context.Context) (V, error); every provider still satisfies the interface via its compile-time assertion."
    - "All 5 provider GetOrSet methods pass the caller's ctx to the setter and keep historical error behavior: redis and valkey keep their cache/redis: / cache/valkey: prefixes, valkey keeps its initErr guard, mem/memcache/postgres return raw setter errors (D-14 — no error-semantics change this phase)."
    - "All existing GetOrSet call sites compile against the new signature: 5 provider test files + the E2E suite; the full module builds."
  artifacts:
    - path: "cache/cache.go"
      provides: "Cache interface GetOrSet with ctx-aware setter (D-03)"
      contains: "setter func(context.Context) (V, error)"
    - path: "cache/doc.go"
      provides: "Package docs updated: ErrCanceled sentinel + SingleflightGetOrSet + ctx-aware GetOrSet"
      contains: "ErrCanceled"
    - path: "cache/mem/mem.go"
      provides: "mem GetOrSet with ctx setter — body stays hand-rolled (Phase 6 delegates to the helper)"
      contains: "setter(ctx)"
    - path: "cache/redis/redis.go"
      provides: "redis GetOrSet signature ripple — cache/redis: prefix unchanged"
      contains: "setter(ctx)"
    - path: "cache/valkey/valkey.go"
      provides: "valkey GetOrSet signature ripple — initErr guard and cache/valkey: prefix unchanged"
      contains: "setter(ctx)"
    - path: "cache/memcache/memcache.go"
      provides: "memcache GetOrSet signature ripple — raw setter error unchanged"
      contains: "setter(ctx)"
    - path: "cache/postgres/postgres.go"
      provides: "postgres GetOrSet signature ripple — raw setter error unchanged"
      contains: "setter(ctx)"
    - path: "cache/cache_e2e_test.go"
      provides: "E2E GetOrSet setter closures with ctx param + context import"
      contains: "func(_ context.Context)"
  key_links:
    - from: "cache/cache.go"
      to: "cache/mem/mem.go"
      via: "compile-time interface assertion var _ cache.Cache[string, any] = (*Cache[string, any])(nil) — fails if the provider GetOrSet signature diverges from the interface"
      pattern: "var _ cache.Cache[string, any]"
  prohibitions:
    - "Never change provider error decoration/prefixes in this phase (D-14) — cache/redis: and cache/valkey: prefixes and valkey's initErr guard stay byte-identical; only the setter signature changes."
    - "Never delegate provider GetOrSet bodies to the SingleflightGetOrSet helper in this phase — body delegation is Phase 6 (SF-03); Phase 5 ripples the signature only."
---

<objective>
Apply the D-03 breaking interface change: the `Cache` interface's `GetOrSet` setter gains `context.Context` — `setter func(context.Context) (V, error)` — and ripple that signature through all 5 provider GetOrSet methods (mem, redis, valkey, memcache, postgres), all provider tests, and the E2E suite so the module stays green.

Purpose: SF-02's helper (plans 05-01/05-02) accepts a ctx-aware setter; this plan makes the public `Cache` contract and every provider consume that same signature. It is the interface-facing half of SF-02 — the mechanism by which the cache package exposes a ctx-setter GetOrSet contract. The compile-time assertions (`var _ cache.Cache[string, any]` in mem.go:130, memcache.go:178, postgres.go:177) force this ripple to land THIS phase, not Phase 6. Provider BODIES stay hand-rolled — delegation to the helper is Phase 6 (SF-03); only the signature changes now.

Output: `cache/cache.go` (interface), 5 provider GetOrSet methods, 5 provider test files, `cache/cache_e2e_test.go`, `cache/doc.go` — all compiling with the ctx-aware setter. No new symbols.

Scope note: 13 files span this plan because the D-03 change is a mechanical signature ripple (5 providers × source+test, plus cache.go, doc.go, cache_e2e_test.go); the 6/6/1 task split keeps no single task overloaded, and every file change is the same setter-signature edit — the elevated plan-total is inherent to the interface change, not an aggregation smell.
</objective>

<execution_context>
@/Users/guionardo/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/guionardo/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-shared-singleflight-helper/05-PATTERNS.md (File Classification table + provider ripple section, lines 299-327)
@.planning/phases/05-shared-singleflight-helper/05-CONTEXT.md (D-03, D-06, D-14)
@cache/cache.go
@cache/mem/mem.go
@cache/redis/redis.go
@cache/valkey/valkey.go
@cache/memcache/memcache.go
@cache/postgres/postgres.go

# Load Skill("spike-findings-go") before implementing (AGENTS.md requirement).
# This plan is wave 1, parallel with plan 05-01 (new helper files). Provider packages do NOT reference the helper this phase, so the two plan scopes are compile-independent.
# Transient compile errors in `make coverage-quick` that reference a parallel plan's in-flight files (singleflight.go / singleflight_test.go) resolve by re-running after that plan lands.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change the Cache interface setter signature and ripple all 5 provider GetOrSet bodies</name>
  <files>cache/cache.go, cache/mem/mem.go, cache/redis/redis.go, cache/valkey/valkey.go, cache/memcache/memcache.go, cache/postgres/postgres.go</files>
  <read_first>cache/cache.go, cache/mem/mem.go, cache/redis/redis.go, cache/valkey/valkey.go, cache/memcache/memcache.go, cache/postgres/postgres.go, .planning/phases/05-shared-singleflight-helper/05-PATTERNS.md</read_first>
  <action>
    In cache/cache.go, change the GetOrSet interface method (line 23) to: `GetOrSet(ctx context.Context, key K, setter func(context.Context) (V, error), ttl ...time.Duration) (V, error)` (D-03) and update its doc comment to state the setter receives a context.Context (the caller's ctx) and is invoked only on a miss. In each of the 5 providers, change the GetOrSet method signature's setter param from `func() (V, error)` to `func(context.Context) (V, error)` and change the internal invocation `computed, err := setter()` to `computed, err := setter(ctx)` (D-06 — the caller's ctx, no separate setterCtx): mem.go:85/91, redis.go:86/96, valkey.go:106 (setter call after the initErr guard), memcache.go:129, postgres.go:128. Preserve every error-decorating line exactly: redis.go keeps `fmt.Errorf("cache/redis: %w", err)` on setter errors, valkey.go keeps the initErr short-circuit (lines 114-117) and `fmt.Errorf("cache/valkey: %w", err)`, mem/memcache/postgres keep their raw setter-error returns (D-14). Do NOT change Get/Set/Delete/Close or any other body logic; do NOT reference SingleflightGetOrSet anywhere in these files (Phase 6). Verify the production tree compiles (go build does not compile test files, so provider tests may still reference the old signature until Task 2). Commit: `feat(05-03): ctx-aware setter in Cache interface and provider GetOrSet`.
  </action>
  <verify>
    <automated>go build ./cache/...</automated>
  </verify>
  <acceptance_criteria>
    - cache/cache.go contains `setter func(context.Context) (V, error)` in the GetOrSet interface method
    - Each provider file (mem.go, redis.go, valkey.go, memcache.go, postgres.go) contains `setter func(context.Context) (V, error)` and a `setter(ctx)` call in GetOrSet
    - redis.go still contains the string `cache/redis: %w`; valkey.go still contains `cache/valkey:` and its initErr guard; mem/memcache/postgres still return the raw setter error (no new fmt.Errorf wrapping)
    - `go build ./cache/...` exits 0
  </acceptance_criteria>
  <done>Interface + all 5 provider signatures and setter calls updated; production tree builds; committed.</done>
</task>

<task type="auto">
  <name>Task 2: Ripple the ctx setter through provider test closures and the E2E suite</name>
  <files>cache/mem/mem_test.go, cache/redis/redis_test.go, cache/valkey/valkey_test.go, cache/memcache/memcache_test.go, cache/postgres/postgres_test.go, cache/cache_e2e_test.go</files>
  <read_first>cache/mem/mem_test.go, cache/redis/redis_test.go, cache/valkey/valkey_test.go, cache/memcache/memcache_test.go, cache/postgres/postgres_test.go, cache/cache_e2e_test.go, .planning/phases/05-shared-singleflight-helper/05-PATTERNS.md (provider test ripple table, lines 315-327)</read_first>
  <action>
    Update every GetOrSet setter closure to accept a context.Context parameter so the tests compile against the new interface (D-03): mem_test.go lines 112, 124, 137 (`func() (string, error)` → `func(context.Context) (string, error)`); redis_test.go line 72 and valkey_test.go line 72 (`func() (string, error) { return "computed", nil }` → `func(context.Context) (string, error) { return "computed", nil }`); memcache_test.go line 64 and postgres_test.go line 83 (same closure shape change). In cache/cache_e2e_test.go, change the three GetOrSet setter closures at lines 78, 90, 102 to `func(_ context.Context) (string, error)` and add the "context" import to its import block (currently errors, testing, time). Keep t.Parallel() placement exactly as-is — the memcache (line 60) and postgres (line 82) get_or_set subtests do NOT call t.Parallel() (server-backed); do not add it. Do not alter assertion logic in any test. Verify with go test on the five provider packages — note these import the cache root package, and Go does not compile an imported package's _test.go files, so this check is isolated from plan 05-01's in-flight test files. cache/cache_e2e_test.go is behind the `e2e` build tag, so neither go test nor go vet without tags compiles it — run `go vet -tags e2e ./cache/` to compile-verify the three closure edits (never string-check them); if that vet fails with a compile error referencing plan 05-01/05-02's in-flight files (singleflight.go / singleflight_test.go), re-run it after those plans land — the wave-1 plans share the cache package build. Commit: `test(05-03): adapt provider GetOrSet test closures to ctx setter`.
  </action>
  <verify>
    <automated>go test ./cache/mem/ ./cache/redis/ ./cache/valkey/ ./cache/memcache/ ./cache/postgres/ -count=1 && go vet -tags e2e ./cache/</automated>
  </verify>
  <acceptance_criteria>
    - All five provider test files contain `func(context.Context)` setter closures passed to GetOrSet; mem_test.go has three (lines 112, 124, 137), redis/valkey one each (line 72), memcache (line 64) and postgres (line 83) one each
    - cache/cache_e2e_test.go contains three `func(_ context.Context) (string, error)` GetOrSet closures and imports "context"
    - `go test ./cache/mem/ ./cache/redis/ ./cache/valkey/ ./cache/memcache/ ./cache/postgres/ -count=1` exits 0 (memcache/postgres/redis/valkey subtests skip gracefully when no server is present)
    - `go vet -tags e2e ./cache/` exits 0 (compiles the e2e-tagged GetOrSet closures — re-run once after plan 05-01/05-02 land if a transient cross-plan compile error appears)
  </acceptance_criteria>
  <done>All provider test closures and E2E closures updated; provider packages test-green; committed.</done>
</task>

<task type="auto">
  <name>Task 3: Update package docs and run the full module gate</name>
  <files>cache/doc.go</files>
  <read_first>cache/doc.go, .planning/phases/05-shared-singleflight-helper/05-PATTERNS.md (doc.go row)</read_first>
  <action>
    In cache/doc.go: extend the Sentinel errors block (lines 31-34) to add `var ErrCanceled = errors.New("cache: canceled")` with a one-line comment noting it wraps the waiter's ctx error (D-09/D-10); extend the interface description (lines 3-5) to state GetOrSet's setter receives a context.Context; add one line mentioning the exported SingleflightGetOrSet helper (D-01) in the providers section or a short paragraph. Keep the indented `//\t` usage-block style. Comments only — no code. Then run the full gate: `go build ./cache/...`, `go test -race ./cache/ ./cache/mem/ -count=1`, `go vet ./cache/`, and `make coverage-quick` (AGENTS.md gate: cache file ≥70%, package ≥80%, total ≥75%). If `make coverage-quick` fails with a compile error referencing plan 05-01's in-flight files (singleflight.go / singleflight_test.go), re-run it after that plan lands — the wave-1 plans share the module build. Commit: `docs(05-03): document ErrCanceled and the shared helper; final ripple gate`.
  </action>
  <verify>
    <automated>go build ./cache/... && go test -race ./cache/ ./cache/mem/ -count=1 && go vet ./cache/ && make coverage-quick</automated>
  </verify>
  <acceptance_criteria>
    - cache/doc.go mentions ErrCanceled and SingleflightGetOrSet; the interface description states GetOrSet's setter receives a context.Context
    - `go build ./cache/...` exits 0
    - `go test -race ./cache/ ./cache/mem/ -count=1` exits 0
    - `go vet ./cache/` exits 0
    - `make coverage-quick` exits 0 (re-run once after plan 05-01 lands if a transient cross-plan compile error appears)
  </acceptance_criteria>
  <done>Package docs updated; full module gate green; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → provider GetOrSet | The setter closure now receives a caller-supplied ctx; the provider passes it through verbatim (D-06) |
| provider → cache.Cache interface | The compile-time assertion is the enforcement point for the D-03 signature contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-10 | Tampering | provider GetOrSet bodies | high | mitigate | Keep every provider body byte-identical except the setter signature + call — no error-prefix changes (D-14), no logic changes; verified by go build + the acceptance-criteria string checks on `cache/redis: %w` / `cache/valkey:` |
| T-05-11 | Elevation of Privilege | Cache interface assertion | medium | mitigate | The three compile-time assertions (`var _ cache.Cache[string, any]`) fail loudly if any provider signature drifts from the interface — the contract is enforced at compile time, not runtime |
| T-05-12 | Tampering | setter ctx propagation | low | mitigate | Providers pass the caller's ctx to the setter exactly once (D-06); a setter never receives a foreign/background ctx — verified by reading the five GetOrSet bodies after edit |
| T-05-SC | Tampering | module deps | low | accept | No new packages this plan; golang.org/x/sync v0.22.0 already pinned (D-15) — no package-legitimacy checkpoint required |
</threat_model>

<verification>
- `go build ./cache/...` exits 0 after Task 1 and Task 3
- `go test ./cache/mem/ ./cache/redis/ ./cache/valkey/ ./cache/memcache/ ./cache/postgres/ -count=1` exits 0 after Task 2
- `go vet -tags e2e ./cache/` exits 0 after Task 2 (e2e-tagged suite compiles — closure changes are compile-verified, not string-checked)
- `make coverage-quick` exits 0 (AGENTS.md gate) at plan end
- No provider file references SingleflightGetOrSet (delegation is Phase 6)
</verification>

<success_criteria>
- The `Cache` interface GetOrSet setter is `func(context.Context) (V, error)` (D-03) and all 5 providers satisfy the interface (compile-time assertions green)
- Every provider passes its caller's ctx to the setter (D-06) with zero error-semantics changes (D-14)
- All provider tests and the E2E suite compile and pass against the new signature
- `cache/doc.go` documents ErrCanceled, the ctx-aware GetOrSet, and the SingleflightGetOrSet helper
- The full module gate (`go build`, race run, vet, coverage-quick) is green — the repo is ready for Phase 6 delegation
</success_criteria>

<output>
Create `.planning/phases/05-shared-singleflight-helper/05-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol / File | Kind | Notes |
|---------------|------|-------|
| `cache.Cache.GetOrSet` | changed interface method | setter `func() (V, error)` → `func(context.Context) (V, error)` (D-03) — breaking public change |
| `cache/mem/mem.go` GetOrSet | modified method | signature + `setter(ctx)`; body hand-rolled (Phase 6 delegates) |
| `cache/redis/redis.go` GetOrSet | modified method | signature + `setter(ctx)`; `cache/redis: %w` prefix kept |
| `cache/valkey/valkey.go` GetOrSet | modified method | signature + `setter(ctx)`; initErr guard + `cache/valkey:` prefix kept |
| `cache/memcache/memcache.go` GetOrSet | modified method | signature + `setter(ctx)`; raw setter error kept |
| `cache/postgres/postgres.go` GetOrSet | modified method | signature + `setter(ctx)`; raw setter error kept |
| 5 provider test files | modified | setter closures gain `context.Context` param |
| `cache/cache_e2e_test.go` | modified | 3 GetOrSet closures gain ctx param; +`context` import |
| `cache/doc.go` | modified | +ErrCanceled sentinel, ctx-aware GetOrSet, SingleflightGetOrSet mention |

*No new symbols are created by this plan — it is a signature ripple + docs. New helper symbols come from plans 05-01 (`SingleflightGetOrSet`, `Do`, `Panic`) and 05-02 (`DoChan`, `ErrCanceled`).*
